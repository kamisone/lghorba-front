// Guest token is now managed server-side as an httpOnly cookie.
// The BFF (/next-api/support/guest/bootstrap) sets it on first bootstrap.
// This module is intentionally empty — kept for import compatibility.
export {};
